// First Day Demo
// Mr. Scott
// Sept 10, 2024

// Interactive Programs -> don't finish...

function setup() {
  //Happens one time, at the start
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  // Happens over and over, targeting 60fps
  //background(220);
  circle(mouseX, mouseY, 20);
}
